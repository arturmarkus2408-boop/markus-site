{
  "name": "markus-site",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "wrangler": "^4.127.0"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  }
}
